# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Backup files from sed/etc.
*.bak

# Claude Code workspace files (local dev only — not part of the deploy).
.claude/

# Netlify CLI cache
.netlify/

# Python bytecode (tools/ scripts import each other)
__pycache__/

# Node (in case a tool ever lands)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment / secrets
.env
.env.*
!.env.example

# Internal lease package (LOI + full consolidated PDF) — not for the public site
review/
