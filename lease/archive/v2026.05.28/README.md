# Archived lease — v2026.05.28

This folder holds the frozen copy of the standard lease as it appeared at tag `lease-v2026.05.28`.

Contents:

- `lease.md` — the lease source at the moment of the tag (Landlord: **Courthouse Square Vashon LLC**).
- `lease.pdf` — the original PDF supplied at draft time. **Heads up: the PDF still names "JT Sheffield LLC" as Landlord** because it predates the entity rename. Replace it with a regenerated PDF (matching the corrected `lease.md`) at your convenience. Until then, `lease.md` is the source of truth for what the lease actually says.

The automated PDF workflow at `.github/workflows/lease-pdf.yml` will produce a corrected PDF the next time a `lease-v*` tag is pushed.
